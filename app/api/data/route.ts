import { NextResponse } from 'next/server'; import fs from 'fs'; import path from 'path'; export async function GET() { try { const dataDir = path.join(process.cwd(), 'data'); const appsData = fs.existsSync(path.join(dataDir, 'apps.json')) ? JSON.parse(fs.readFileSync(path.join(dataDir, 'apps.json'), 'utf8')) : []; const blogPosts = fs.existsSync(path.join(dataDir, 'blog.json')) ? JSON.parse(fs.readFileSync(path.join(dataDir, 'blog.json'), 'utf8')) : []; return NextResponse.json({ appsData, blogPosts }); } catch (error: any) { return NextResponse.json({ error: error.message }, { status: 500 }); } }
